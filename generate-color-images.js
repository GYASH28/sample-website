// Deprecated intentionally.
//
// Fakhri Mart no longer generates one product image per colour. The current
// storefront keeps one verified product photograph and applies a lightweight
// browser-only colour preview layer so customers can explore colours without
// creating, downloading, storing or maintaining duplicate image files.
//
// IMPORTANT: digital preview colours are visual references only. They are not
// supplier-stock claims and must never be copied into product.colors unless a
// current supplier shade card/source explicitly verifies them.

console.log([
  "Colour-image generation is retired.",
  "Use src/components/ShadePreviewStudio.jsx for browser-based previews.",
  "No files were generated or modified.",
].join("\n"));
